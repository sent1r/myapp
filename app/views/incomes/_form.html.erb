<%= form_for(@income) do |f| %>
  <% if @income.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@income.errors.count, "error") %> prohibited this income from being saved:</h2>

      <ul>
      <% @income.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">

  </div>
  <div class="field">
    <%= f.label 'Категория' %><br>
    <%= f.select(:category_id, @cat.collect {|p| [p.name, p.id]}) %>
  </div>
  <div class="field">
    <%= f.label 'Наименование' %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label 'Величина'%><br>
    <%= f.text_field :cost %>
  </div>
  <div class="actions">
    <%= f.submit "Подтвердить" %>
  </div>
<% end %>
